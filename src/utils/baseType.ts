export type DialogModel = {
    title: string,
    visible: boolean,
    height: number,
    width: number,
}

export type DrawerModel = {
    title: string,
    size: string,
    destoryOnClose: boolean,
    confirmText: string,
    visible: boolean,
}